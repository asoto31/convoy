'use strict';

/**
 * @ngdoc function
 * @name convoyApp.controller:EpisodiospodcastCtrl
 * @description
 * # EpisodiospodcastCtrl
 * Controller of the convoyApp
 */
angular.module('convoyApp').controller('EpisodiosPodcastCtrl', function ($scope, $mdSidenav, episodios) {
	$mdSidenav('left').close();
});